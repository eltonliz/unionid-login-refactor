# AGENTS.md — 一键登录重构

> 项目规则与 AI 协作契约。改动本仓库前必须先读本文件与 [MEMORY.md](MEMORY.md)。

## 项目定位
这是一个用于老板、产品、研发和测试共同评审的高保真逻辑原型，聚焦 APP 与微信小程序基于 UnionID 的账号识别、手机号绑定、冲突处理和租户配置。本项目是可点击的业务原型，不是可上线的微信登录实现。

## 技术栈与命令
- 技术栈：单文件 HTML + CSS + Vanilla JavaScript，无外部依赖。
- 包管理器：无。
- 开发：`python3 -m http.server 4173` → `http://localhost:4173/`
- 构建：无构建步骤，`index.html` 为交付产物。
- 测试/检查：使用 Playwright 或等价浏览器自动化检查页面错误、核心交互与多视口布局。
- 发布：本期不发布，本地原型交付。
- 环境注意：不读取或写入任何微信密钥、AppSecret、token 或真实用户数据。

## 目录
- `index.html` — 账号登录重构逻辑与可交互高保真原型。
- `index-v2-product-flow.html` — V2 产品流程演示备份。
- `UnionID登录重构-需求说明.md` — 面向研发和测试的完整业务规则、场景流程、异常与验收用例。
- `登录重构-内部评审逐字稿.md` — 按原型页面顺序演示的研发、测试评审讲稿。
- `product-facts.md` — 微信 UnionID、OpenID、手机号能力的官方事实基线。
- `MEMORY.md` — 长期业务规则与设计取舍。

## 编码约定
- 业务主键始终使用内部 `user_id/account_id`；UnionID、`appid + openid`、手机号都是身份凭证。
- 禁止未验证手机号或用户不知情时静默合并；微信手机号授权或短信验证确认本人号码后，才可进入当前项目的受控合并流程。
- 主账号和资产迁移规则未定前，只记录合并候选，不得演示合并成功、来源账号失效或资产迁移。
- APP 与小程序跨端不比较裸 OpenID；OpenID 必须与 AppID 成对存储。
- 业务规则必须保持登录顺序无关、幂等、可审计。
- 保持单文件、无外部依赖；不为演示引入框架。
- 视觉保持飞书 / Notion / 企业 SaaS 式克制感；不用 emoji，不用装饰性数据，不滥用渐变和图标。

## 验证与交付
1. 页面在桌面端和移动端视口下无横向溢出、无白屏、无 JavaScript 错误。
2. 租户开关、流程切换、身份命中模拟器、CASE 切换和冲突弹窗必须可实际点击。
3. 验收逻辑必须覆盖同 UnionID 异手机号、同手机号异 UnionID、两个已有账号冲突、拒绝授权、重复授权和租户隔离。
4. 交付时说明改动、验证结果、限制与 `MEMORY.md` 更新情况。

## 边界（不做的事）
- 不调用真实微信 OAuth、`code2Session` 或手机号接口。
- 不实现真实账号、订单、余额、积分、券或会员权益迁移。
- 不设计微信密钥管理、正式数据库表和生产 API。
- 不做破坏性 git 操作，不修改无关文件。
- 不写入任何凭据、密钥、账号或真实用户数据。
