# MEMORY.md — 一键登录重构项目记忆

> 业务背景与已确认口径。改动业务逻辑前先读本文件；只记长期信息，不记任何 secret。

## 业务背景
租户 SaaS 存在 APP 手机号登录、APP 微信授权登录和微信小程序登录。当同一用户在多端使用不同入口时，需要将可验证的身份稳定地解析到同一个内部业务用户，同时防止不同人的订单、会员、收藏、余额和权益被错误合并。

## 已确认规则（改动前必读）
1. 内部 `user_id/account_id` 是业务主键；UnionID、`appid + openid`、已验证手机号都是外部身份凭证。
2. APP 与小程序跨端识别使用同一微信开放平台账号命名空间下的 UnionID，不比较裸 OpenID。
3. 租户配置项命名为“微信登录是否强制绑定手机号”；它仅控制微信登录的手机号要求，不决定手机号能否作为已验证登录凭证。
4. 手机号 + 验证码通过后不创建正式用户，只写临时访问记录（手机号、端、项目、验证方式、时间）；取得 UnionID 后才在当前 `tenant_id + project_id` 创建或查找正式用户。
5. 同 UnionID 异手机号时暂不合并，保留当前项目内两个账号及原数据，等待后续取得相同的已验证手机号后重新判断。
6. 修改手机号、首次下单收货地址、通讯录邀请/填写手机号等业务号码可以触发“手机号碰撞”和合并候选，但不能静默成为账号凭证；建议再通过微信手机号授权或短信验证确认，同号才合并、异号继续等待。该折中授权方式仍待负责人最终确认。
7. UnionID 与手机号分别命中不同项目账号时，不新建第三个账号；手机号一致且已验证时进入当前项目合并，不一致时停止当前操作并等待一致。
8. 项目账号合并必须幂等、可审计、可恢复，只处理当前 `tenant_id + project_id` 的账号和资产；主账号选择和各类资产迁移规则仍需专项确认。
9. 所有账号均为项目级账号，不存在全局账号；账号查找、身份绑定、合并和资产迁移必须同时限定 `tenant_id + project_id`。同一 UnionID 进入不同项目时分别形成不同项目账号，不跨项目复用或合并。
10. 手机号临时访问用户拒绝或取消微信授权时，流程终止并退出；不创建正式用户、不保留正式登录态、不产生半绑定数据，临时访问记录可保留审计。
11. 手机号临时访问用户点击进入直播、下单、立即购买、收藏、分享、我的六类事件时必须完成微信授权；通讯录不在强制授权列表，它属于开关关闭时的手机号匹配触点。
12. 当前不提供客服工单、人工双账号核验或自助解绑；满足手机号一致且已验证条件时，由系统执行当前项目账号合并。
13. 开关关闭时，个人中心修改手机号、首次下单收货地址、通讯录邀请/填写手机号均可触发手机号碰撞；生成合并候选后支持号码列表多次选择，确认同号后进入当前项目合并。
14. 开关从关闭切换为开启时，需要检查当前项目已保存手机号记录；可信号码可进入匹配，业务候选号转为待确认，具体是否立即批处理仍待负责人确认。
15. 两种登录方式 × APP/小程序 × 开关开/关共 8 种组合必须分别验收。

## 版本边界（不做）
- V1 仅完成逻辑规则与高保真交互演示。
- 不实现真实微信 API、数据库迁移、历史账号清洗和客服合并后台。
- V1 不支持一个业务账号绑定多个主手机号或多个主 UnionID。

## 架构与技术取舍
- 推荐概念模型：`phone_visit_record` 管手机号临时访问，`project_user` 管取得 UnionID 后的正式用户，`user_merge_candidate` 管等待号码一致、授权确认和最终合并结果；所有判断必须带 `tenant_id + project_id`。
- 架构师提供的 `usercenter.sql` 包含用户、微信身份和租户项目客户数据三类表；产品口径不因表名中的 `platform_user` 推导出全局账号，所有账号仍以项目为边界。
- 本期原型先演示临时访问、UnionID 正式用户、候选匹配和合并主流程；资产盘点必须覆盖代理、店员、店长、买家，以及红包、现金红包、收藏、足迹、订单、售后、积分、优惠券、会员、佣金、店铺关系和行为数据。
- 登录成功前必须在当前 `tenant_id + project_id` 内完成项目账号查找、账号状态及黑名单校验；不可通过创建新账号绕过停用、注销、删除或拉黑状态。
- 原型使用纯 HTML/CSS/JavaScript，避免为一页逻辑演示引入构建链和依赖。

## 已知坑
- “先 UnionID → 再手机号 → 最后 OpenID”不是正确的跨端决策树；OpenID 不能作为 APP 与小程序的兜底比较项。
- “同 UnionID 但手机号不同则新建用户”会造成身份唯一性冲突。
- “业务表单出现同手机号就静默合并”会把收货人、通讯录联系人误当账号本人；业务号码应先生成候选，再完成微信手机号授权或短信验证确认。
- “允许获取手机号”和“强制绑定手机号”不是同一个配置语义。
- `channel_key` 的业务含义及与 AppID、开放平台、租户、项目的关系尚未确认，不能默认互相等同。
- 用户表与租户项目客户表同时存在手机号字段，项目账号登录凭证的唯一事实来源和同步规则仍需架构确认。
- 当前 SQL 的 openID 唯一约束未包含 AppID；除非 `channel_key` 就是 AppID，否则与“AppID + openID”产品规则不一致。
- 当前 SQL 未明确项目级账号唯一约束及合并去向；重复进入、重复授权和重复合并存在创建重复账号或重复迁移风险。
- 当前 SQL 的客户表索引引用了未定义的 `vip_time` 字段，建表前必须由架构师确认补字段或删除索引。
- 不使用 `PHONE_ONLY / WECHAT_ONLY / FULL` 作为新的账号状态；手机号和微信是否齐全由身份关系判断。项目合并处理中、成功、失败及来源账号失效状态仍需架构补充。
- 手机号碰撞只是生成合并候选的条件，完成本人确认后才允许进入项目合并；资产迁移仍必须具备项目范围、幂等、审计、对账和失败恢复。
- “同手机号异 UnionID”虽然可进入当前项目合并，但合并后两个微信身份是都保留为登录凭证，还是其中一个失效，负责人尚未定案；未确认前不得直接覆盖或删除任一 UnionID。

## 外部资源
- 微信 UnionID 机制：https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/union-id.html
- 微信手机号快速验证组件：https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/getPhoneNumber.html
- 用户提供的原始参考页：https://4m44a9pga4bnz.doubaoapps.com/app/app_17epxtb1g8u

## 变更记录
- 2026-09-24：根据负责人会议纪要复核将 `index.html` 升级为 V5：推翻“手机号登录立即创建正式用户”，改为手机号临时访问记录 + UnionID 创建正式用户；关键事件由 7 类修正为 6 类，通讯录移至开关关闭时的手机号触点；新增业务手机号候选确认、8 种组合矩阵、开关由关变开、三类逻辑记录，以及代理/店员/店长/买家和完整资产清单。项目级隔离保留，依据为负责人此前单独确认。
- 2026-09-24（历史口径，已被 V5 取代）：V4.1 曾按“手机号创建正式用户、七类关键事件”补齐 13 个详细场景；V5 已将其修正为手机号临时记录、六类关键事件和业务手机号候选确认。
- 2026-09-24（部分历史口径，已被 V5 取代）：负责人当时确认项目级账号、同 UnionID 异手机号等待一致和项目内合并；其中“手机号登录创建正式账号、七类关键事件、业务号码不触发匹配”已由后续纪要复核推翻。
- 2026-09-24（历史口径，已被 V4 取代）：曾按架构师 AI 分析保留平台账号概念，并排除关键事件强制微信授权、手机号修改和自动合并；负责人后续已明确改为项目级账号、七类关键事件强制授权及满足条件后项目内合并。
- 2026-09-24（历史口径，已被 V4 取代）：V3.1 曾采用平台账号—微信身份—租户项目客户身份三层表述；最新规则已取消“全局/平台账号”的产品概念，SQL 表名仅作为实现命名参考。
- 2026-09-24（历史口径，已被 V4 取代）：曾将账号冲突统一拦截且不再合并；最新规则为手机号一致且来源已验证时合并当前项目账号，否则等待手机号一致。
- 2026-09-24：重构交付《登录授权场景流程图-v1.2.0.html》：13 个场景（每场景 触发→流程图→关键判断→成功/不满足/拒绝失败→重复→测试重点）+ 可点击交互（租户开关、身份判断矩阵、CASE1/2/3 切换、冲突弹窗与工单幂等、返回/重置）；删除关键事件统一微信授权与一切自动合并/自助解绑迁移设计；Playwright 实测 13 场景切换、开关分支、矩阵 5 组合、工单重复提交拦截、移动端/桌面端布局与控制台零报错。
- 2026-09-24：将 `index.html` 升级为 V3 场景流程台，13 个业务场景均可独立切换流程图，并补充租户开关、身份命中模拟、7 项评审定案和冲突工单弹窗；V2 保留为 `index-v2-product-flow.html`。
- 2026-09-24：将需求说明压缩为 V1.1 精简流程版，保留 13 个独立业务场景并为每个场景补充 Mermaid 流程图；原 749 行详细版保留为 `UnionID登录重构-需求说明-v1-详细版.md`。
- 2026-09-24：新增面向研发和测试的《UnionID登录重构-需求说明》，按场景明确 unionID、openID、手机号的判断顺序、异常处理和 31 条验收用例。
- 2026-09-24：在产品流程演示中补充微信登录、手机号登录、账号冲突和统一账号判断的可验收规则逻辑。
- 2026-09-24：将技术规则台改版为产品评审流程演示，以微信登录、手机号登录、账号冲突三张流程图为主，并将双手机号案例改写为产品语言。
- 2026-09-24：完成 UnionID 登录重构可交互高保真原型，覆盖租户手机号策略、两类登录流程、身份命中决策、三组双手机号 CASE、冲突处理、研发契约与验收标准。
- 2026-09-23：初始化项目规则，固化 UnionID 与手机号的确定性身份解析口径。
